- name: Test de mon module
  hosts: localhost
  vars:
    secret_id: "23a6e457-6bac-43f0-9fc8-0b62031ea466"
    secret_name: "test2" 
  connection: local
  tasks:
  - name: get secret by name
    get_secret:
      name:  "{{ secret_name }}"
      
    register: output
  - name: dump test output
    debug:
      var: output.message
