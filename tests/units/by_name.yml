- name: Test de mon module
  hosts: localhost
  vars:
    secret_id: "23a6e457-6bac-43f0-9fc8-0b62031ea466"
    name: "hello_there" 
  connection: local
  tasks:
  - name: access secret by name
    access_secret:
      name:  "{{ name }}"
      
    register: output
  - name: dump test output
    debug:
      var: output.message
